import logging 

app_title = "ITOA Automation Orchestration REST API"

logging.basicConfig(level=logging.INFO, filename='team_automation_orchestratio_api.log', format='%(asctime)s - %(levelname)s - %(message)s', filemode='a+')
log=logging.getLogger()
